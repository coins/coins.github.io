{
    "plugins": [
        "plugins/markdown",
        "plugins/summarize"
    ],
    "source":
    {
        "include": ["."],
        // "includePattern": ".js",
        "excludePattern": "(test|spec|docs)"
    },
    "opts":
    {
        "template": "templates/default", // same as -t templates/default
        "destination": "./docs/", // same as -d ./out/
        "recurse": true// same as -r
    }
}